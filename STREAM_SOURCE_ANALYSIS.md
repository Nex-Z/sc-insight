# 按主播获取公开直播源：实测分析

最新进展：纯 HTTP + FFmpeg 已对当前直播完成实录，并验证自动分段续录与停止。
有效逻辑来自播放器的时间处理控制器，早期检查的旧解析器不是当前生效分支。
当前实现和验收入口见 [HTTP_RECORDING.md](HTTP_RECORDING.md)。下文保留早期排查过程，其中“尚未打通”等结论已被本次实录结果更新。

验证日期：2026-09-12。检查了用户当前打开的直播间，以及数据库中的另一位公开在线主播。用户正在使用的浏览器和直播页面没有被刷新、关闭或修改。

## 已证实的发现链路

1. 以正常 HTTP GET 获取公开直播间 HTML，使用 `Accept: text/html` 和当前正常浏览器已经使用的 User-Agent。两位主播均返回 HTTP 200；初次没有 HTML Accept 的请求返回了 406，不能据此认为必须运行浏览器。
2. 页面服务端初始数据包含 `streamName`、`isCamAvailable`、`isOnline`、`status`，以及播放器的 `hlsStreamHost`、`hlsStreamUrlTemplate`。
3. 当前页面返回的模板是：

   ```text
   https://edge-hls.{cdnHost}/hls/{streamName}/master/{streamName}{suffix}.m3u8
   ```

4. 两个正常播放器实际使用的自动清晰度 suffix 都是 `_auto`。将页面提供的主机和 streamName 代入上述模板，得到的主清单路径与实际浏览器请求一致。这里的 suffix 来自观察；不能把两次观察当成平台永久契约。
5. 直接通过 HTTP 获取两个主清单均返回 200 和 `#EXTM3U`。此步骤没有传 Cookie、Authorization 或生成签名，也没有打开可见浏览器。因此，按主播后台发现主清单不需要 CDP。

## 尚未证实的录制兼容性

两个主清单都出现自定义 `#EXT-X-MOUFLON` 标记；播放器的后续媒体清单请求带有 `psch`、`pkey`。匿名浏览器能正常产生这些请求，但不应据参数名断言它们的算法或直接标记为 DRM。

将正常匿名浏览器实际产生的媒体清单 URL 和请求头原样交给当前 FFprobe，两位主播均在 18 秒测试期限内未返回媒体轨道。结论仅是：这条 FFmpeg 直接录制链路尚未验证通过；超时本身不能排除网络、参数、播放器格式等原因，也不能证明永远不支持。

公开 HLS 测试流录制通过，并不能代替该平台当前格式的兼容验证。本次没有修改签名、处理自定义编码、请求私密内容或开始录制主播视频。

## 对实现的影响

- 主播源解析器可以后台读取公开页面初始数据，确认公开在线状态，然后使用页面返回的配置取得主清单；没有必要每次要求用户手动打开浏览器。
- 不应仅凭 `.m3u8` 或 HTTP 200 把来源标记为“录制就绪”。解析成功和 FFmpeg 媒体探测成功应是两个独立状态。
- 稳定缓存应以平台与主播标识为键，保留最新源信息与失败状态；录制任务启动前重新验证在线状态和媒体可读性。
- 当前平台格式的实际录制兼容性需要单独解决。在验证成功前，不应把解析结果自动塞入现有队列并报告功能完成。

脱敏的两份实测结果保存在 `artifacts/stream-acquisition-analysis.json`，其中不包含 pkey 值、Cookie 或 Authorization。

## 后续验证与多路部署约束

后台浏览器原生录制曾做过短片验证，但该方案已按用户要求撤销，未接入队列；不会把逐路播放浏览器作为 Linux 多路录制的运行方案。

进一步用纯 HTTP 解析得到的主清单测试，FFprobe 能读出媒体轨道，但两个不同主播的子清单都包含 `EXT-X-ENDLIST`，时长均约 20.6 秒，分片路径都是各 CDN 路径下的 `cpa/v2/chunk_*.m4s`。这些响应不能当作已取得当前主播的实时直播。因此，HTTP 200 和 FFprobe 成功都不足以单独证明来源正确。脱敏结果位于 `artifacts/direct-source-comparison.json` 和 `artifacts/direct-probe-diagnosis.json`。

已新增 `server/public-source.js`：不启动浏览器，通过公开 SSR JSON 和页面播放器配置解析主清单，拒绝非公开、不可访问或不匹配的主播，不执行页面脚本，不生成授权参数。自定义格式明确返回 `unsupported_manifest` 和 `recordable:false`，没有自动排队录制替代内容。

可执行 `npm run source:check -- 主播名称` 单独验证 HTTP 解析，未就绪时退出码为 2；解析错误为 1。此命令不依赖数据库或已打开的浏览器。

录制核心默认改为 FFmpeg `-c copy`，避免逐路重编码。`RECORDING_CONCURRENCY` 可配置为 1–32（默认 2），`RECORDING_TRANSCODE=1` 才启用原有转码路径；应用这些 Worker 配置需要重启服务。FFmpeg / FFprobe 默认从 PATH 查找，也可使用已有路径环境变量，代码未加入 Windows 专用运行依赖。Linux 尚未做实机验证。

## CDP 续调：请求重放与清单解析分离

使用用户重新打开的 `http://127.0.0.1:9222`，仅监听已有直播页面，不导航、不录屏。捕获低延迟 HLS 请求后，在 Node 中保留其实际 URL 与请求头进行 HTTP 重放，只过滤 HTTP/2 伪头及连接/压缩传输字段。

最新可复现结果：浏览器与 HTTP 返回内容逐字节相同，状态 200，清单带有持续更新的媒体序列且没有 ENDLIST。普通分片引用均指向重复的 `../media.mp4`，同时出现 `#EXT-X-MOUFLON:PSCH`、`EXT-REF` 和 `URI` 字段。这把问题定位到平台自定义清单/地址解析，而不是当前请求 Cookie 缺失或 HTTP 客户端没有拿到相同内容。

已查看公开播放器包中的清单解析逻辑；公开包包含专门的解析器。旧版社区实现主要处理 `FILE` 字段，不能据此推定已经支持当前 `URI / EXT-REF` 变体。离线适配实验尚未还原当前真实分片地址，没有接入生产录制，也没有修改/生成 pkey。

新增可重复诊断命令：

```text
npm run source:debug -- http://127.0.0.1:9222
```

命令只附加已有浏览器作诊断，不启动窗口或录制；原样重放实际请求，报告清单结构、占位地址数、媒体序列和内容一致性，不输出 Cookie、pkey 或完整媒体 URL。需要地址适配时返回退出码 2；运行错误返回 1。浏览器在命令结束后保持打开。

剩余未完成工作：独立实现并验证当前自定义分片地址格式，确认还原地址与正常播放器请求一致，再做 HTTP + FFmpeg 录制、持续拉流及多路测试。在这之前，主清单解析成功仍然不等于录制成功。
