# 主播高光录制

主播详情 → **高光时刻** → 开启“高光录制”。默认关闭，按主播独立保存设置；开启时一并启用主播监控，关闭高光不会取消监控或删除历史片段。详情页可查看触发时间、原因、状态、时长，并播放、下载已归档视频，历史按每页 20 条向前翻页。目标进度提醒可独立开启，不要求开启录制。

## 默认规则

- 必须连续观察公开直播至少 5 分钟，首次接入、重连、离线、非公开状态或采样失败/超过 20 秒缺口后重新预热。关注/监控采集间隔需为 5–10 秒。
- 人数：最近 1 分钟均值相对此前最多 5 分钟的样本均值增加至少 30% 且至少 50 人，并在最近至少 20 秒持续超过阈值。
- TK：最近 30 秒公开打赏至少 100 TK，且达到之前 5 分钟折算到 30 秒速率的 3 倍；或单笔至少 500 TK。两种情况都需连续聊天覆盖完整比较窗口（5 分 30 秒）。
- 不计聊天历史回放；聊天覆盖中断、金额未知或消息接收延迟超过 30 秒时不据此触发 TK 高光。它不是总收入或到账数据。
- 任一信号触发；相同片段内合并各类原因，保存该类最新触发值。阈值可在详情页调整。

## 目标进度提醒与目标高光

关注或监控后，进度从未完成值跳到 100% 或以上时自动生成站内完成通知。“目标即将完成提醒”开关控制额外的接近完成提醒，默认阈值为剩余 ≤ 1%，可设 0.1–20%。两种阶段每个目标各一次，直接跳满仅发完成提醒。当前界面仅使用站内通知，详见 [关注主播功能](INSIGHTS.md)。

服务端后台定期读取公开目标，无需保持主播详情页打开；公开信息缓存为 10 秒。状态与去重信号持久化，重启和重复采样不会重复提醒。目标更换、金额归零/明显回退可开始新一轮；目标消失、关闭或首次已经完成不推断新完成事件，观察缺口超过 30 秒也不推断何时完成。上游在两次采样间完成并重置且没有返回完成状态时，可能漏报。仅确认公开目标进度，不声称某个人单笔付清。

开启高光录制后，“目标高光”默认开启：接近完成时保留已有前 2 分钟缓存并等待完成；确认完成后继续录 15 分钟。直接跳满同样从已有缓存开始。它不受人数 / TK 的 5 分钟预热限制，前置画面不足时仅保留实际已有部分。人数、TK、目标同时触发，只升级或延长同一条高光；连续目标也并入当前录像，延长到最后一次完成后 15 分钟。等待目标时没有普通高光的 15 分钟总长上限，仍受磁盘、断流、关闭和非公开状态保护；目标被撤销/更换或数据断档时结束等待，不伪造完成时间。

每个主播在数据库中最多一条录制/归档中的高光。同一缓存会话的后续片段起点不早于上一段已归档结束时间，避免再次保存同一段前置缓存。不同主播仍可各自录制，整体最多 2 路高光缓存。

## 视频与资源

复用公开直播解析和 HTTP 中转，独立 FFmpeg 按约 10 秒关键帧边界写 TS。平时保留约 140 秒滚动缓存；普通人数 / TK 高光触发后锁定前 120 秒并继续接收后 180 秒，再封装 MP4。连续触发延长同一片段，普通片段最长 15 分钟，结束后冷却 30 秒；目标事件可越过普通冷却并遵循上述目标规则。切点按分片边界，时长可能有数秒偏差，非逐帧剪辑。长时间等待目标的已锁定分片不会因滚动索引超过 20 分钟而丢失。

高光缓存独立最多 2 路，额外占用带宽/进程；与已有整场录制可以同时开启，会分别拉流。更多目标等待名额。启动要求至少 1 GB 可用空间；运行低于 512 MB 或 45 秒没有新完整分片时停止并尝试保留现有高光。下线、非公开、停止或正常退出时，未满后录时长的片段标为“已中断”。

文件在配置录像目录的 `highlights/<model_id>/`，缓存在 `.highlight-cache/<随机目录>/`。正常归档或关闭会清理当前临时缓存；归档失败保留缓存，数据库 `highlights.cache_directory` 可定位。异常重启会将未完成记录标为中断，已有 MP4 探测成功后可播放；尚未封装的 TS 保留供人工恢复，不自动拼接。未命中但因异常退出遗留的临时目录也不会自动删除。FFmpeg 由父进程连接守护，服务意外退出后停止写入。

## 接口与验证

- `GET /api/models/:modelId/highlights?before=<id>`：设置、运行状态、高光列表和 `nextBefore`。
- `PUT /api/models/:modelId/highlights`：`{"enabled":true,"config":{"viewerRatio":0.3,"viewerIncrease":50,"tipMinimum":100,"tipRatio":3,"singleTip":500,"goalRecord":true,"goalNotify":true,"goalNearPercent":1}}`。
- `GET /api/highlights/:id/file`：支持 HTTP Range；`?download=1` 下载。
- `npm run db:init`：幂等建表；启动服务也会确保高光表存在。
- `npm test`：预热、人数突增/抖动、TK、采样/聊天断档、历史回放、目标跳满/重置/去重、完成后 15 分钟合并、长缓存保留和输入校验。
- `npm run test:highlights`：本地合成 HLS → 真实 FFmpeg 缓存 → 信号合并/最长时长/冷却 → MP4 归档与失败保留 → 数据库/API/Range，清除自建数据库夹具。
- `npm run test:highlights:ui`：需先运行上项生成播放夹具；隔离页面验证开关、阈值保存、视频实际播放和手机布局。无线上主播请求。

当前验收使用本地合成媒体和事件，未等待真实主播发生自然高光；线上信号准确率和阈值需在实际监控中观察。
