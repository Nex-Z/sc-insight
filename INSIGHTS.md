# 关注主播：通知、回顾与内容整理

## 入口

- **提醒通知**：上下线、目标完成、高光、公开资料变化及原有规则事件。支持类型筛选、未读筛选、单条已读、全部已读和历史分页。点击通知直达场次数据、高光或资料历史。
- **主播详情 → 统计数据 → 直播场次 → 回顾本场**：观看人数、公开 TK、目标与高光事件按时间展示，点击可定位覆盖该时间的录像。当前场次每 10 秒刷新。
- **主播详情 → 资料变化**：简介、状态文案、菜单及公开相册变化，以及官网关注人数、评分和评分人数的快照。
- **主播详情 → 统计数据 → 与上一周相比**：两个完整 7 天的覆盖时长、活跃天数、观看均值、常见开播时段及每小时互动。
- **侧栏内容库 / 主播详情 → 内容库**：统一检索录像和高光，保存标题、标签、收藏、已看状态、备注、时间标记和片段区间。片段播放到终点自动暂停，原视频不变。

## 通知口径

关注或监控后自动启用站内上下线与目标完成通知，不需要另建上线规则。上下线必须由连续、无采集失败的明确布尔状态变化确认；首次发现已在线、未知状态和长采集缺口不补发通知。旧上线规则不再额外生成重复上线提醒，观看人数阈值规则继续生效。

高光通知在实际创建高光录制时产生，同一片段延长不重复通知。需要先为主播启用高光录制，并获得缓存名额。录制中通知可先查看状态，归档后自动更新为可播放。目标完成通知不依赖高光录制；“目标即将完成提醒”开关仅控制接近完成阶段。上游在两次采样间完成并重置目标时仍可能漏报。

通知保存在 PostgreSQL，已读状态为本站共享状态，不再依赖某个浏览器的本地已读指针。本版界面仅使用站内通知，不请求浏览器系统通知权限。全部已读只处理点击时已获取的最新事件 ID，之后的新事件保留未读。

## 历史与时间关联

资料后台每分钟检查待采集队列，每位关注或监控主播约每 30 分钟采集一次，每轮最多 20 位；网络失败或目标较多会延迟。首次成功读取建立基线，不宣称过去的变化。部分接口失败或结构异常会保留该部分最后成功值，不推断删除。相册只反映接口当前返回的公开照片，新增可见不等于刚上传；不下载原图到本地。

两个比较周期以 UTC+8 今日零点为界，不包含今天。观看均值按有效时间加权；两期各至少覆盖 3 天和 1 小时有效人数数据才给出涨跌。常见开播时段要求该小时至少观察到 3 次明确开播。互动按对应聊天采集时长折算，历史回放不计入每小时指标。公开 TK 不代表总收入。

录像与场次通过主播 ID 和记录时间交集关联；时间跳转使用录像开始时间加媒体时长，属近似定位，尤其整场录制可能存在启动延迟和媒体断流偏差。未覆盖或未归档的时间不跳到其他录像。高光按实际归档开始时间定位。没有视频时仍能查看场次数据与事件。片段区间是播放标记，当前不导出剪辑文件。

## 启动与验证

现有安装下次启动会幂等补充事件字段、资料历史和内容元数据表；全新安装仍需先执行 `npm run db:init`。内容元数据只记录整理信息，不移动、覆盖或清理已有视频。

```powershell
npm test
npm run test:insights
```

`test:insights` 使用配置数据库内独立的临时 schema，测试后只删除该 schema。它验证真实 SQL、重复初始化、上下线连续性和失败抑制、目标去重、资料失败保留、高光通知、场次关联、有效周期比较、内容持久化、分页、已读和 Range。随后用本地合成 MP4 验证浏览器跳转、定位、片段终点暂停及五个手机布局。不会启动线上采集或修改已有主播与录像；截图在 `artifacts/insight-check/`。自然直播触发准确率仍需实际运行观察。
